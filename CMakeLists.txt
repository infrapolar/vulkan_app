cmake_minimum_required(VERSION 3.26)
project(vulkan_app)
set(CMAKE_CXX_STANDARD 20)
file(GLOB source_files "src/*.cpp" "src/*/*.cpp")
find_package(glfw3)
find_package(glm)
find_package(Boost)
add_executable(vulkan_app ${source_files})
target_link_libraries(vulkan_app glfw vulkan)